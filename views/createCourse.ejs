<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="csrf-token" content="<%= csrfToken %>">
  <link rel="stylesheet" href="./css/styles.css" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" href="/images/icons8-books-doodle-favicons/Web/icons8-books-doodle-16.png" />
  <title><%= title %></title>
</head>
<body class="bg-gray-100 min-h-screen">
  <div class="grid grid-cols-4">
    <div class="col-start-2 col-span-2">
      <%- include('header.ejs') %>

      <div class="bg-white p-8 shadow-md rounded-lg mt-6">
        <div class="flex justify-between items-center border-b-2 pb-2 mb-4">
          <h2 class="text-2xl font-bold text-gray-800">Create New Course</h2>
          <div class="text-right">
            <p class="text-sm font-semibold text-gray-600"><%= currentUser.firstName %> <%= currentUser.lastName %></p>
            <a href="/signout" class="text-green-600 text-lg font-semibold hover:underline">Signout</a>
          </div>
        </div>

        <a href="/teacher-dashboard" class="text-blue-600 text-sm font-semibold hover:underline mb-4 block">← Back to Dashboard</a>

        <form action="/createcourse" method="post" autocomplete="on" class="space-y-5">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">

          <div>
            <label for="courseName" class="block text-gray-700 font-medium">Course Name:</label>
            <input type="text" name="courseName" id="courseName" class="mt-1 w-full p-2 border border-gray-300 rounded" required />
          </div>

          <div>
            <label for="courseDescription" class="block text-gray-700 font-medium">Course Description:</label>
            <textarea name="courseDescription" id="courseDescription" class="mt-1 w-full p-2 border border-gray-300 rounded h-32" required></textarea>
          </div>

          <div>
            <button type="submit" class="w-full bg-green-600 text-white py-2 rounded hover:bg-green-700 font-semibold transition">Create Course</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
